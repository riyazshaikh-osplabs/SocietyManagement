const logger = {
    log: (...message) => {
        console.log(...message);
    },
    error: (...message) => {
        console.log(...message);
    },
    debug: (...message) => {
        console.log(...message);
    }
}

export default logger;